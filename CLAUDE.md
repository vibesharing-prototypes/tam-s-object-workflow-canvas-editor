# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The full project spec is in [`build-day-spec.md`](build-day-spec.md) — read it before making significant changes.

## Commands

```bash
pnpm dev          # starts Vite frontend + Hono backend concurrently
pnpm build        # production build
pnpm typecheck    # tsc --noEmit
```
